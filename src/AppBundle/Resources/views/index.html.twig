{% extends 'layout.html.twig' %}

{% block body %}

<h2>Réservation</h2>
   <a href="{{ path('booking_new') }}"><button>Creér une réservation</button></a>
   <a href="{{ path('booking_index') }}"><button>Voir les réservations</button></a>

<h2>Client</h2>
    <a href="{{ path('client_new') }}"><button>Creér un client</button></a>
    <a href="{{ path('client_index') }}"><button>Voir les clients</button></a>


<h2>Voiture</h2>
    <a href="{{ path('car_new') }}"><button>Créer une voiture</button></a>
    <a href="{{ path('car_index') }}"><button>Voir les voitures</button></a>

<h2>Options</h2>
    <a href="{{ path('option_new') }}"><button>Créer une couleur ou une gamme</button></a>

{% endblock %}