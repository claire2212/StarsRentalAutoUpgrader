{% extends 'base.html.twig' %}

{% block body %}
    <h1>Car</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ car.name }}</td>
            </tr>
            <tr>
                <th>Available</th>
                <td>{% if car.available %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ car.color.name }}</td>
            </tr>
        </tbody>
    </table>

    
    <a href="{{ path('car_index') }}"><button>Retour à la liste</button></a>

    <a href="{{ path('car_edit', { 'id': car.id }) }}"><button>Modifier</button></a>

    {{ form_start(delete_form) }}
        <input type="submit" value="Delete">
    {{ form_end(delete_form) }}

{% endblock %}
